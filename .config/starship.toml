"$schema" = 'https://starship.rs/config-schema.json'

format = """
[](#C2A1D3)\
$os\
[](bg:#F4A6B8 fg:#C2A1D3)\
$username\
[](fg:#F4A6B8 bg:#F4D58D)\
$directory\
[](fg:#F4D58D bg:#86BBD8)\
$git_branch\
$git_status\
$git_metrics\
[](fg:#86BBD8 bg:#7FCAE5)\
$c\
$cpp\
$rust\
$python\
$nodejs\
$java\
[](fg:#7FCAE5 bg:#A8D5BA)\
$cmd_duration\
[](fg:#A8D5BA bg:#F29191)\
$time\
[ ](fg:#F29191)\
$line_break\
$character\
"""

# ───────────────────────────────────────────────────────────────────
# CONFIGURATION GÉNÉRALE
# ───────────────────────────────────────────────────────────────────

# Désactiver la ligne vide au début
add_newline = false

# ───────────────────────────────────────────────────────────────────
# SYSTÈME D'EXPLOITATION
# ───────────────────────────────────────────────────────────────────

[os]
style = "bg:#C2A1D3 fg:#FAF8F5"
disabled = false
format = '[ $symbol ]($style)'

[os.symbols]
Arch = "󰣇"
EndeavourOS = "🏔️"
Debian = " "
Fedora = " "
Ubuntu = " "
Windows = " "
Macos = " "

# ───────────────────────────────────────────────────────────────────
# UTILISATEUR
# ───────────────────────────────────────────────────────────────────

[username]
show_always = false
style_user = "bg:#F4A6B8 fg:#3D4E5C"
style_root = "bg:#F29191 fg:#FFFFFF"
format = '[ $user ]($style)'
disabled = false

# ───────────────────────────────────────────────────────────────────
# RÉPERTOIRE COURANT
# ───────────────────────────────────────────────────────────────────

[directory]
style = "bg:#F4D58D fg:#3D4E5C"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"
truncate_to_repo = true
read_only = " 󰌾"
read_only_style = "bg:#F4D58D fg:#F29191"

[directory.substitutions]
"CLionProjects" = " "
"AdaProjects" = " "
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
"Videos" = " "
"Desktop" = " "
".config" = " "

# ───────────────────────────────────────────────────────────────────
# GIT - Branche
# ───────────────────────────────────────────────────────────────────

[git_branch]
symbol = " "
style = "bg:#86BBD8 fg:#3D4E5C"
format = '[ $symbol$branch ]($style)'
truncation_length = 20
truncation_symbol = "…"

# ───────────────────────────────────────────────────────────────────
# GIT - Status
# ───────────────────────────────────────────────────────────────────

[git_status]
style = "bg:#86BBD8 fg:#3D4E5C"
format = '[$all_status$ahead_behind ]($style)'
ahead = " ⇡${count} "
behind = " ⇣${count} "
diverged = " ⇕⇡${ahead_count}⇣${behind_count} "
untracked = " ?${count} "
modified = " !${count} "
staged = " +${count} "
renamed = " »${count} "
deleted = " ✘${count} "

# ───────────────────────────────────────────────────────────────────
# GIT - Métriques
# ───────────────────────────────────────────────────────────────────

[git_metrics]
disabled = false
ignore_submodules = true
added_style = 'bg:#86BBD8 fg:#A8D5BA'
deleted_style = 'bg:#86BBD8 fg:#F29191'
format = '[$added/$deleted ]($added_style)'
only_nonzero_diffs = true

# ───────────────────────────────────────────────────────────────────
# LANGAGES DE PROGRAMMATION
# ───────────────────────────────────────────────────────────────────

[c]
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'
detect_extensions = ['c', 'h']

[cpp]
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'
detect_extensions = ['cpp', 'cc', 'cxx', 'hpp', 'hxx']

[rust]
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'

[python]
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version)(\($virtualenv\)) ]($style)'
pyenv_version_name = true

[nodejs]
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'

[java]
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'

[golang]
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'

[elixir]
disabled = true
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'

[elm]
disabled = true
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'

[haskell]
disabled = true
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'

[julia]
disabled = true
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'

[nim]
disabled = true
symbol = "󰆥 "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'

[scala]
disabled = true
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol($version) ]($style)'

# ───────────────────────────────────────────────────────────────────
# DURÉE DE LA COMMANDE
# ───────────────────────────────────────────────────────────────────

[cmd_duration]
style = "bg:#A8D5BA fg:#3D4E5C"
format = '[ ⏱ $duration ]($style)'
show_milliseconds = false
show_notifications = false

# ───────────────────────────────────────────────────────────────────
# HEURE
# ───────────────────────────────────────────────────────────────────

[time]
disabled = false
time_format = "%R"  # Format 24h : HH:MM
style = "bg:#F29191 fg:#FAF8F5"
format = '[ 🍓 $time ]($style)'

# ───────────────────────────────────────────────────────────────────
# CARACTÈRE DE PROMPT
# ───────────────────────────────────────────────────────────────────

[character]
success_symbol = '[❯](bold #86BBD8)'
error_symbol = '[❯](bold #F29191)'
vimcmd_symbol = '[❮](bold #C2A1D3)'

# ───────────────────────────────────────────────────────────────────
# LINE BREAK
# ───────────────────────────────────────────────────────────────────

[line_break]
disabled = false

# ───────────────────────────────────────────────────────────────────
# ENVIRONNEMENTS SPÉCIFIQUES
# ───────────────────────────────────────────────────────────────────

[docker_context]
symbol = " "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol$context ]($style)'
only_with_files = true

[kubernetes]
symbol = "☸ "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol$context(\($namespace\)) ]($style)'
disabled = true

[package]
symbol = "📦 "
style = "bg:#7FCAE5 fg:#3D4E5C"
format = '[ $symbol$version ]($style)'
disabled = true

# ───────────────────────────────────────────────────────────────────
# BATTERY
# ───────────────────────────────────────────────────────────────────

[battery]
disabled = true

[[battery.display]]
threshold = 10
style = 'bold red'

[[battery.display]]
threshold = 30
style = 'bold yellow'

# ───────────────────────────────────────────────────────────────────
# JOBS EN ARRIÈRE-PLAN
# ───────────────────────────────────────────────────────────────────

[jobs]
symbol = "✦ "
style = "bg:#F4A6B8 fg:#3D4E5C"
format = '[ $symbol$number ]($style)'
number_threshold = 1
symbol_threshold = 1

# ───────────────────────────────────────────────────────────────────
# MÉMOIRE USAGE
# ───────────────────────────────────────────────────────────────────

[memory_usage]
disabled = true
threshold = 75
symbol = " "
style = "bg:#F29191 fg:#FAF8F5"
format = '[ $symbol$ram ]($style)'

# ───────────────────────────────────────────────────────────────────
# STATUS
# ───────────────────────────────────────────────────────────────────

[status]
style = "bg:#F29191 fg:#FAF8F5"
symbol = "✗ "
format = '[ $symbol$status ]($style)'
disabled = false
